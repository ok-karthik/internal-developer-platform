# CODEOWNERS for <org>/[[ .TeamName ]]-apps
#
# Root of the application source tree: 3-tenant-workloads/[[ .TeamName ]]/apps/ in
# this monorepo, the repository root once split out.
#
# There is no platform-owned carve-out here. Application source is entirely the
# team's; the platform's leverage is the golden path that generated it, not review.
* @acme-corp/[[ .TeamName ]]
