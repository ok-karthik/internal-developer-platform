# CODEOWNERS for <org>/team-a-infra
#
# Root of the infra tree: 3-tenant-workloads/team-a/infra/ in this monorepo,
# the repository root once split out.
#
# Last matching pattern wins, so the general rule goes first.

# The team owns its services' infrastructure claims — each apps/<app>/<env>/*.tf is
# a thin call into a version-pinned, platform-blessed module.
*                       @acme-corp/team-a

# Provider config, backend state wiring, and the team IAM role are platform-owned:
# these decide what the team's Terraform is *able* to do.
/platform/              @acme-corp/platform-engineering
