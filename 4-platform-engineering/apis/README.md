# APIs

Empty until Phase 5 of `PLAN.md`.

This is where platform API definitions — KRO `ResourceGraphDefinition`s or Crossplane
`XRD`s/`Composition`s — will live once a composition engine is (re)introduced. An earlier
Crossplane experiment was archived and later deleted (`git log -- archived/crossplane/`);
whichever engine replaces it belongs here, not in a directory named `archived`.
