server:
  extraArgs:
    - --insecure

metrics:
  enabled: true
  serviceMonitor:
    enabled: true

controller:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

configs:
  cm:
    url: "https://argocd.localhost"
    oidc.config: |
      name: Keycloak
      issuer: https://keycloak.localhost/realms/platform
      clientID: argocd
      clientSecret: $oidc.keycloak.clientSecret
      requestedScopes: ["openid", "profile", "email", "groups"]
  rbac:
    policy.default: role:readonly
    policy.csv: |
      # Regenerated per team by onboard-team once the scaffolder consumes
      # this (documented follow-up, Phase 7.3 -- 2-idp-scaffolder is out of
      # scope on this branch). tenant-a rows below are the seed pattern: copy-
      # paste per team until that automation exists.
      p, role:tenant-a-developer, applications, get,      tenant-a/*, allow
      p, role:tenant-a-developer, applications, sync,     tenant-a/*, allow
      p, role:tenant-a-developer, logs,         get,      tenant-a/*, allow
      p, role:tenant-a-developer, applications, delete,   tenant-a/*, deny
      p, role:tenant-a-developer, applications, override, tenant-a/*, deny
      g, platform:team-a:developer, role:tenant-a-developer
