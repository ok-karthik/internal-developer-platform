# 4. Platform APIs

Empty until Phase 5 (previously Phase 9 ranking, see `PLAN.md`'s Interlude) of `PLAN.md`.

CRDs / kro `ResourceGraphDefinition`s — the custom APIs tenants call — will live here once
a composition engine is (re)introduced. An earlier Crossplane experiment was archived and
later deleted (`git log -- archived/crossplane/`); whichever engine replaces it belongs
here, not in a directory named `archived`.
